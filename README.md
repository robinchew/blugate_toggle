EnableNotifications callback
============================
Generated by Brave AI

Copying the buffer works because it decouples the data's lifetime from the driver's internal memory management, preventing the callback from accessing invalid or overwritten memory.

1\. Buffer Reuse and Overwrite
------------------------------
The BLE driver in TinyGo (and most embedded stacks) typically uses a single static buffer or a small pool of buffers to receive incoming packets to save RAM.

The Problem: When the callback is triggered, buf is a slice header pointing to this shared internal buffer. As soon as the callback returns (or even while it is executing if a new packet arrives), the driver reuses or overwrites this same memory location for the next incoming notification. 
Why fmt.Printf Triggers It: fmt.Printf is relatively slow due to formatting and I/O operations. If a second BLE packet arrives while fmt.Printf is still processing the first one, the driver overwrites the shared buffer. Your fmt.Printf call then reads corrupted or mixed data, which can cause internal panics (e.g., invalid UTF-8 sequences) or logic errors that kill the goroutine. 
The Fix: copy() creates a distinct clone of the data in a new memory location you control. This ensures the data remains stable even if the driver immediately reuses its internal buffer for the next packet. 

2\. Stack vs. Heap and Escape Analysis
--------------------------------------
Escape Analysis: When you pass buf directly to fmt.Printf, the compiler's escape analysis might behave unpredictably in TinyGo's constrained environment, potentially trying to reference stack memory that becomes invalid once the callback context shifts. 
Explicit Allocation: make([]byte, len(buf)) forces an explicit allocation (usually on the heap in TinyGo for dynamic sizes). This guarantees the memory persists independently of the callback's stack frame, which might be small or managed differently in interrupt/C-callback contexts. 

3\. Preventing Data Races
-------------------------
In a concurrent environment (BLE interrupts + main loop), accessing the driver's internal buffer directly is a data race.

Without Copy: The driver writes to buf (interrupt context) while fmt.Printf reads from buf (goroutine context) simultaneously. This is undefined behavior and often leads to silent crashes on microcontrollers. 
With Copy: The copy operation happens immediately at the start of the callback, creating a snapshot. The rest of your code processes this safe snapshot, eliminating the race condition. 

Summary
-------
The silent death occurs because fmt.Printf keeps the buffer reference alive too long, allowing the BLE driver to overwrite the data underneath it or trigger a data race. Copying the buffer creates a safe, immutable snapshot that isolates your processing logic from the driver's aggressive memory reuse.

Test Bluetooth is working
=========================

`bluetoothctl list` if this command seems to freeze and do nothing, then try `sudo systemctl start bluetooth`
